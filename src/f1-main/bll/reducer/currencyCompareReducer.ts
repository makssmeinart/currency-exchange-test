import {createSlice} from "@reduxjs/toolkit";

const currencyCompareSlice = createSlice({
    name: "compare",
    initialState: {},
    reducers: {}
})

export const currencyCompareReducer = currencyCompareSlice.reducer